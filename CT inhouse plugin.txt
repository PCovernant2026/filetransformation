ctp_solution is an option set field on the opportunity table 

ctp_subsolutions is a lookup field on the opportunity table that looksup to the subsolution table (ctp_subsolutions)
logig should be when sub solution is selected query and populate the architect (ctp_architect) assigned to that sub_solution note: the ctp_architect is a lookup feild to the user table


ON creation of the opportunity 

query the creating user, check their title (title) on the user record if title contains Partner, populate the new_clientpartnerid field on the opportunity form. note new_clientpartnerid is a lookup field on the opportunity
 table that also looks up to the user table.


When we close a deal as won, automatically set the "Stage" field to 100%
