You are a senior Microsoft Dynamics 365 / Dataverse developer.

Write a PRODUCTION‑READY C# Dataverse plugin for the Opportunity table that synchronizes two fields whose option set VALUES DO NOT MATCH numerically:

• Custom Lost Reason (logical name: new_lostreason)
• OOB Status Reason (logical name: statuscode)

IMPORTANT:
- The option labels are the same, but the numeric option values are DIFFERENT.
- You MUST translate values using an explicit mapping.
- A direct value copy is NOT valid.

REQUIREMENTS:
1. The plugin must run on Opportunity UPDATE
2. Execution must be SYNCHRONOUS and PRE‑OPERATION
3. The plugin must:
   - When new_lostreason changes, map its option value to the correct statuscode value
   - When statuscode changes, map its option value to the correct new_lostreason value
4. Implement TWO mapping dictionaries:
   - new_lostreason → statuscode
   - statuscode → new_lostreason
5. Prevent infinite loops using a Depth check
6. Use a Pre‑Image to confirm that a value actually changed
7. Do NOT overwrite fields unnecessarily
8. Do NOT rely on labels
9. Fail safely if an unmapped value is encountered (do not throw unless instructed)
10. Include:
    - Full plugin C# code
    - Clear comments explaining logic
    - Clean Dataverse‑idiomatic structure

ASSUME:
• Entity logical name: opportunity
• Custom field logical name: new_lostreason
• OOB Status Reason logical name: statuscode
• Message: Update
• Stage: Pre‑Operation
• Mode: Synchronous
• Pre‑Image name: PreImage
• Pre‑Image attributes: statuscode, new_lostreason
• .NET Framework 4.6.2
• Sandbox‑safe

OPTION SET MAPPINGS (EXACT):

new_lostreason → statuscode
118290002 → 118290005  // Lost - Competitor
118290005 → 118290002  // Lost - Late to bid
118290006 → 118290001  // Lost - Not Pursuing
118290001 → 118290006  // Lost - Other
118290004 → 118290003  // Lost - Pricing
118290003 → 118290004  // Lost - Specifiable differences unable to meet

statuscode → new_lostreason
118290005 → 118290002
118290002 → 118290005
118290001 → 118290006
118290006 → 118290001
118290003 → 118290004
118290004 → 118290003

DO NOT include:
• JavaScript
• Power Automate or workflows
• Pseudo‑code
• Assumptions about aligned option values

Return ONLY:
• The complete C# plugin code
• Brief plugin registration instructions
