Write a PRODUCTION‑READY C# Dataverse plugin that keeps two fields on the Opportunity table perfectly in sync:

• OOB Status Reason (logical name: statuscode)
• Custom Lost Reason (logical name: new_lostreason, Choice column)

REQUIREMENTS:
1. The plugin must run on Opportunity UPDATE
2. Execution must be SYNCHRONOUS and PRE‑OPERATION
3. The plugin must:
   - When new_lostreason changes, copy the SAME option value to statuscode
   - When statuscode changes, copy the SAME option value to new_lostreason
4. Both option sets already contain IDENTICAL values (1:1 mapping by option value)
5. The plugin must work for:
   - D365 UI
   - Native "Close as Lost" button
   - Power Pages / Portal closes
   - API, integrations, and imports
6. Prevent infinite loops using a Depth check
7. Use a Pre‑Image to ensure the value actually changed
8. Do NOT overwrite fields unnecessarily
9. Do NOT rely on labels — use option values only
10. Include:
    - Full C# plugin code
    - Proper null checks
    - Clear comments explaining the logic

ASSUME:
• Entity logical name: opportunity
• Custom field logical name: new_lostreason
• OOB field logical name: statuscode
• Message: Update
• Stage: Pre‑Operation
• Execution Mode: Synchronous
• Pre‑Image name: PreImage
• Pre‑Image attributes: statuscode, new_lostreason
• .NET Framework 4.6.2
• Sandbox‑safe plugin

DO NOT include:
• JavaScript
• Workflows or Power Automate
• Placeholder or pseudo‑code

Return ONLY:
• The plugin C# code
• Brief plugin registration instructions
